# WAMCast

Neural-operator WAM-IPE surrogate for storm-time thermospheric mass-density forecasting with calibrated uncertainty.

## Install

```bash
pip install -e .
```

Documentation coming in v0.1.0.
